interface:
  display_name: "FindReason RAG Attribution"
  short_description: "Run FindReason badcase attribution from pasted cases or Fornax traces"
  default_prompt: "Use $findreason-rag-attribution to inspect trace artifacts first, compare workflow input/output with origin -> rerank -> prompt document survival and answer alignment, then choose the needed CLI/probe evidence before explaining the primary cause."
